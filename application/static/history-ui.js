// Source snapshots are separate from provisional statistics derived from imported matches.
function historyHome(){
 const t=tournament,chosen=params.get('snapshot')||'决赛',snap=t.snapshots.find(s=>s.name===chosen)||t.snapshots[2],champion=t.snapshots[2].rows[0];
 $('content').innerHTML=`<section class="history-heading"><div><span class="badge">已完结 · 历史赛事</span><h1>${esc(t.name)}</h1><p>冠军 · ${esc(champion.name)} <strong>${fmt(champion.total)} PT</strong></p></div><a class="button" href="${href('results')}">${t.results.length} 场赛果 →</a></section>${homeNextMatchDay()}<div class="stage-nav">${t.snapshots.map(s=>`<a class="button" href="${href('home',{snapshot:s.name})}" aria-current="${s.name===chosen?'page':'false'}">${esc(s.name)}</a>`).join('')}</div><div class="history-layout"><section><div class="section-head"><h2>${esc(snap.name)}队伍榜</h2><span class="badge">阶段积分</span></div><div class="card">${snap.rows.map((r,i)=>`<div class="rank-row"><span class="identity"><span class="rank">${i+1}</span><span><strong>${esc(r.name)}</strong><small style="display:block">${r.carry===null?'常规赛积分':`带入 ${fmt(r.carry)} · 本阶段 ${fmt(r.total-r.carry)}`} · ${i?`距前一名 ${((snap.rows[i-1].total-r.total)/10).toFixed(1)}`:'积分领先'}</small></span></span><strong class="num ${cl(r.total)}">${fmt(r.total)}</strong></div>`).join('')}</div></section><section><div class="section-head"><h2>最近赛果</h2><a href="${href('results')}">全部 →</a></div>${sortedResults().slice(0,2).map(resultCard).join('')}<a class="button" href="${href('players',{stage:chosen==='决赛'?'final':chosen==='半决赛'?'semi':'regular'})}" style="margin-top:16px">查看本阶段个人榜 →</a></section></div>`+historyDaily(chosen);
}

function historyDaily(chosen){const id=chosen==='决赛'?'final':chosen==='半决赛'?'semi':'regular';const days=(tournament.dailySummaries||[]).filter(d=>d.stageId===id);return days.length?`<section><h2>比赛日队伍积分</h2>${days.map(d=>`<div class="card" style="padding:20px;margin:12px 0"><strong>比赛日 ${d.number} · ${esc(d.day)}</strong>${d.detailAvailable?'':'<small> · 仅当日合计</small>'}${d.entries.map(e=>`<div class="rank-row"><span>${esc(team(e.teamId)?.name)}</span><strong>${fmt(e.points)} PT</strong></div>`).join('')}</div>`).join('')}</section>`:''}
